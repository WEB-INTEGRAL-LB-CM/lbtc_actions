name: Hello World with WorkFlow 

on: 
  workflow_dispatch: 

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello World"

      - name: Decir-fecha
        run: date
